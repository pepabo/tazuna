apiVersion: tazuna.pepabo.com/v1
kind: GenesisSecret
spec:
  secrets:
    - items:
        accessKeyID:
          mapTo: AWS_ACCESS_KEY_ID
  outputs:
    - stdout: {}
      kubernetesSecret:
        name: bad-secret
        namespace: default
